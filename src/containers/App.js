import React from 'react';
import RequestMatcher from "../components/RequestMatcher";
import Grid from './Grid';

const App = () => (
    <div>
        <RequestMatcher/>
        <Grid/>
    </div>
);

export default App
